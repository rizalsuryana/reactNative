export type LoginForm = {
  email: string;
  password: string;
};

export type RegisterType = {
  // id: number;
  fullName: string;
  email: string;
  password: string;
  confirmPassword: string;
  agreement: boolean;
};
